{
  "scripts": {
    "react-server": "cd client && npm start",
    "helloWorld-server": "cd servers && nodemon helloWorld.js",
    "start": "concurrently \"cd client && npm run dev\" \"cd servers && npm start\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3"
  }
}
